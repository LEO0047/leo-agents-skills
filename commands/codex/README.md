# commands/codex

**Codex** slash commands。

目前無收錄。第一個 command 進來時直接放在這個目錄底下。

## 收錄條件

- 單檔 `.md` 形式的 Codex slash command
- 安裝位置：`~/.codex/commands/<command>.md`（若 Codex 支援該機制）

詳見根目錄 [`docs/SKILL_SPEC.md`](../../docs/SKILL_SPEC.md)。
