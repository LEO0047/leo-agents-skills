# commands/claude

**Claude Code** slash commands（`/<command>`）。

## 收錄條件

- 純 prompt 型 slash command（單檔 `.md`，不是目錄）
- 使用 Claude Code 約定的 frontmatter：`name`、`description`、`argument-hint`、`disable-model-invocation`
- 安裝位置：`~/.claude/commands/<command>.md`

詳見根目錄 [`docs/SKILL_SPEC.md`](../../docs/SKILL_SPEC.md)。
