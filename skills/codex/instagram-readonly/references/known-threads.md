# Resolve a conversation without a public contact list

No private contacts, account names, thread IDs, or saved Direct URLs are included in this public package.

Resolve the exact conversation from the live inbox after verifying unread state. Use a private mapping supplied for the current task only when its identity and unread state can be rechecked. Never guess IDs, publish the mapping, or open an unread thread to discover its URL.
